NEXT_PUBLIC_REQUEST_URL=http://localhost:8080

# OAuth Configuration (Optional)
# These are fetched from the server's /oauth/config endpoint
# but can be overridden here for development
# NEXT_PUBLIC_OAUTH_ENABLED=false
# NEXT_PUBLIC_OAUTH_REDIRECT_URL=http://localhost:8080/oauth/callback
